# This is an example .env file used to store environment variables.
# Environment variables are used to configure your application without hardcoding values.
# Common uses include storing API keys, feature toggles, port numbers, database URLs, etc.

# Example variables:
# API_KEY=your_api_key_here
# FEATURE_TOGGLE=true
# PORT=8080

# To use these variables in your Python code, you can use the `python-dotenv` package.
# Here's a short example:

# from dotenv import load_dotenv
# import os

# load_dotenv()  # loads variables from .env into environment
# api_key = os.getenv("API_KEY")
# print(f"API Key is: {api_key}")
